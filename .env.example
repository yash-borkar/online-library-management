# Shared by docker-compose for frontend + backend JWT signing (min 32 characters).
AUTH_SECRET=change-me-to-a-long-random-string-at-least-32-chars

# Optional: host port mapped to the API container (default 14000). Container still listens on 4000.
# BACKEND_PUBLISH=14000
